import DesignSystemsPage from "./DesignSystemsPage";

export const metadata = {
  title: "Structured Editorial · Design Systems · layout.page",
  description:
    "A complete grayscale-first Design System for structure-led websites. Type, color, spacing, and components in one system — light by default; dark and color optional.",
};

export default function Page() {
  return <DesignSystemsPage />;
}
